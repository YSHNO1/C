#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    char s[201];
    while(gets){
        int l = strlen(s);
        int i;
        for(i=l-1;i>=0;i--){
            printf("%c",s[i]);
        }
        printf("\n");
    }
}
